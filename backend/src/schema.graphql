
type Query {
    hello(name: String): String!,
    getLocation(location: String!): String
}

type Mutation{
    register(last_name: String!, first_name: String!, age:Int!, location: String!): Boolean
}
